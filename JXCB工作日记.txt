1.顺序抄表:
	a.进入到抄表详情页面后进行抄表。填写读数和抄表图片后，点击保存会保存抄表员位置，返回主界面选择抄表上传会上传抄表数据到服务器。更新抄表数据到CameraCB表，Trail表中插入一条抄表员轨迹。
	此处表Trail对于需求中的CBMapTrail(需要统一，暂时保留)
	b.更新了显示信息内容，字体大小和抄表图片显示位置，增加了进入页面后，切换上下一户后，以及点击保存后自动弹出软键盘方便输入读数
	c.增加软键盘确认事件，可以在弹出来的软键盘中直接点击回车保存
	d.增加图片显示（可以放大缩小）
	e.已超表中增加图片的显示、增加手机本地图片缓存拍照图片。（已保存到本地相册）

2.标签更新：
	更新或者解绑标签后插入一条日志。

3.特殊抄表页面修改：
	a.页面修改
	b.水表数据显示


3.19

		1. 去掉系统设置；清除缓存 功能放到登录后的抄表设置里
		3. 测试连接 和 保存设置都去掉，只放一个保存按钮（保存的时候测试连接如果连接	  不成功不保存并且提示）

		1. 虚拟目录 去掉（在程序中写死）
	10. 标签更新
		1. 暂无数据 改为 无未上传的信息
		2. 右上角的竖3点改为 + （加号）
		3. 点击竖3点弹出来的选择按钮尽量做得类似支付宝中的 + 点出来的效果
		4. 标签更换  改为 标签更新
		5. 标签更换中 标签编号  改为 原标签号 
		6. 标签更换中 如果用户查询出来没有绑定标签编号则标签编号处显示未绑定
		7. 标签更换中 新表签号输入框的提示词 改为 NFC读取新标签号
		8. 标签解绑中  变更信息与 新标签号 无需显示
	6.
		1. 编号改为户号
		2. 图片列有图显示无图（图片显示还可以调优）
		3.修改抄表详情页面显示信息


未完成：
	1.
	4.
	5.

	4.权限逻辑问题：不考虑重新排序，考虑弹出提示
	5.回车事件，VIVO手机出现过一次BUG，但是现在能用，华为之前能用，现在BUG
	6.离线状态导致的未绑定或者解绑状态，网络恢复后统一上传，可能会导致本地的avrrage表的nfccardid 未更新



3.20

		设备报损：
			1.报损信息提交后，有网则直接上传，没网则保存到本地。
			2.每次进入报损信息查看页面，如果有网，则提示正在更新本地报损信息，然后查看服务器，更新到本地。如果没网则提示当前网络未连接，显示的是本地。


	

		

